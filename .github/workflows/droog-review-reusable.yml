name: Droog AI Review (Reusable)

on:
  workflow_call:
    inputs:
      pr_number:
        description: 'PR number to review'
        required: true
        type: string
      post_comments:
        description: 'Post comments to PR'
        required: false
        type: boolean
        default: true
      auto_fix:
        description: 'Generate auto-fix suggestions'
        required: false
        type: boolean
        default: false
      auto_apply:
        description: 'Automatically apply low-risk fixes'
        required: false
        type: boolean
        default: false
    secrets:
      GEMINI_API_KEY:
        required: true

jobs:
  review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
    
    steps:
      # Step 1: Checkout DroogAI repository (to build it)
      - name: Checkout DroogAI
        uses: actions/checkout@v4
        with:
          repository: abhijeet1771/DROOGAI
          path: .droog-ai
          ref: master
      
      # Step 1.5: Verify DroogAI checkout
      - name: Verify DroogAI checkout
        run: |
          if [ ! -d ".droog-ai" ]; then
            echo "❌ DroogAI directory not found after checkout"
            exit 1
          fi
          if [ ! -f ".droog-ai/package.json" ]; then
            echo "❌ package.json not found in DroogAI repository"
            echo "Current directory: $(pwd)"
            echo "Contents of .droog-ai:"
            ls -la .droog-ai/ | head -20
            exit 1
          fi
          echo "✅ DroogAI checkout verified - package.json found"
      
      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: .droog-ai/package-lock.json
        continue-on-error: true
      
      # Step 3: Install DroogAI dependencies
      - name: Install DroogAI dependencies
        working-directory: .droog-ai
        run: |
          echo "📦 Installing DroogAI dependencies..."
          if [ -f "package-lock.json" ]; then
            npm ci
          else
            npm install
          fi
          echo "✅ Dependencies installed successfully"
      
      # Step 4: Build DroogAI
      - name: Build DroogAI
        working-directory: .droog-ai
        run: npm run build
      
      # Step 5: Verify build
      - name: Verify DroogAI build
        working-directory: .droog-ai
        run: |
          if [ ! -f "dist/index.js" ]; then
            echo "❌ DroogAI build failed - dist/index.js not found"
            exit 1
          fi
          echo "✅ DroogAI built successfully"
      
      # Step 6: Checkout calling repository (the repo being reviewed)
      - name: Checkout calling repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      # Step 6.5: Verify .droog-ai still exists after checkout
      - name: Verify .droog-ai directory exists
        run: |
          if [ ! -d ".droog-ai" ]; then
            echo "❌ .droog-ai directory not found after checkout"
            echo "Current directory: $(pwd)"
            echo "Contents of workspace:"
            ls -la
            exit 1
          fi
          if [ ! -f ".droog-ai/dist/index.js" ]; then
            echo "❌ DroogAI build not found in .droog-ai/dist/index.js"
            exit 1
          fi
          echo "✅ .droog-ai directory verified - dist/index.js found"
      
      # Step 7: Load index if exists (from calling repo)
      - name: Load index if exists
        id: load-index
        run: |
          if [ -f ".droog-embeddings.json" ]; then
            echo "index_exists=true" >> $GITHUB_OUTPUT
            echo "✅ Codebase index found"
          else
            echo "index_exists=false" >> $GITHUB_OUTPUT
            echo "ℹ️  No codebase index found (will skip cross-repo duplicate detection)"
          fi
        continue-on-error: true
      
      # Step 8: Run DroogAI Review on calling repository
      - name: Run DroogAI Review
        id: review
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Verify .droog-ai exists and change to it
          if [ ! -d ".droog-ai" ]; then
            echo "❌ .droog-ai directory not found"
            echo "Current directory: $(pwd)"
            ls -la
            exit 1
          fi
          cd .droog-ai
          echo "✅ Changed to .droog-ai directory: $(pwd)"
          
          PR_NUMBER="${{ inputs.pr_number }}"
          POST_FLAG=""
          AUTO_FIX_FLAG=""
          AUTO_APPLY_FLAG=""
          
          if [ "${{ inputs.post_comments }}" = "true" ]; then
            POST_FLAG="--post"
          fi
          
          if [ "${{ inputs.auto_fix }}" = "true" ]; then
            AUTO_FIX_FLAG="--auto-fix"
          fi
          
          if [ "${{ inputs.auto_apply }}" = "true" ]; then
            AUTO_APPLY_FLAG="--auto-apply"
          fi
          
          echo "🚀 Running DroogAI review on ${{ github.repository }} PR #${PR_NUMBER}"
          echo "   Flags: --enterprise ${POST_FLAG} ${AUTO_FIX_FLAG} ${AUTO_APPLY_FLAG}"
          
          # Run review with enterprise features enabled
          node dist/index.js review --repo "${{ github.repository }}" --pr "${PR_NUMBER}" --enterprise ${POST_FLAG} ${AUTO_FIX_FLAG} ${AUTO_APPLY_FLAG} || true
          
          # Copy report.json to calling repo root for artifact upload
          # Note: report.json is created in current directory (.droog-ai)
          if [ -f "report.json" ]; then
            cp report.json "${{ github.workspace }}/report.json"
            echo "✅ Review report generated and copied to workspace root"
          else
            echo "⚠️  report.json not found in .droog-ai directory"
            echo "Current directory: $(pwd)"
            echo "Files in current directory:"
            ls -la | head -10
            echo "Creating minimal error report in workspace root"
            echo '{"success":false,"error":"Review failed to generate report","totalIssues":0,"issuesBySeverity":{"high":0,"medium":0,"low":0},"comments":[],"timestamp":"'$(date -u +"%Y-%m-%dT%H:%M:%SZ")'"}' > "${{ github.workspace }}/report.json"
          fi
      
      - name: Upload review report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: review-report
          path: report.json
          retention-days: 7
          if-no-files-found: warn

